'use client';
import { BrowserRouter, Route, Routes } from 'react-router-dom';

// pages

const Router = () => {
    return (
        <BrowserRouter>
            <Routes>
                <Route />
            </Routes>
        </BrowserRouter>
    );
};

export default Router;
